import logo from "../../../../assets/logo.png"

const Logo=()=>{
    return(
        <div className="logo">
            <img src={logo} alt="Growise Logo"></img>
        </div>
    );
};
export default Logo;